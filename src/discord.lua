local _M = require("discord.module")

_M.client = require("discord.client")
-- This shouldn't be needed for normal use, but it's here anyway just to be safe.
_M.rest = require("discord.rest")

return _M